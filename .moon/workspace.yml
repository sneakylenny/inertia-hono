$schema: 'https://moonrepo.dev/schemas/workspace.json'

projects:
  - 'apps/*'
  - 'packages/*'

vcs:
  provider: github
  defaultBranch: main

versionConstraint: '>=1.0.0'

# Single-commit repos have no `HEAD~1`; avoid git-based hashing until history exists.
hasher:
  walkStrategy: 'glob'
