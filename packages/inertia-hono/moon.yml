$schema: 'https://moonrepo.dev/schemas/project.json'

id: 'inertia-hono'
language: typescript
layer: library
stack: backend
tags:
  - 'publishable'

dependsOn:
  - 'inertia-server'

tasks:
  build:
    command: 'bun run build'
    deps:
      - 'inertia-server:build'
    inputs:
      - 'src/**/*'
      - 'package.json'
      - 'tsconfig.json'
      - 'tsconfig.build.json'
      - 'project://inertia-server'
    outputs:
      - 'dist'

  test:
    command: 'bun run test'
    deps:
      - 'inertia-server:build'
    inputs:
      - 'src/**/*'
      - 'package.json'

  lint:
    command: 'bun run lint:inertia-hono'
    options:
      runFromWorkspaceRoot: true
    inputs:
      - 'src/**/*'
      - '/eslint.config.js'
