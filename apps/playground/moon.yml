$schema: 'https://moonrepo.dev/schemas/project.json'

language: typescript
type: application
stack: backend

dependsOn:
  - 'inertia-hono'

tasks:
  dev:
    command: 'bun run dev'
    preset: server
    deps:
      - 'inertia-hono:build'
    inputs:
      - 'src/**/*'
      - 'package.json'
