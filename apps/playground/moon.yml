$schema: 'https://moonrepo.dev/schemas/project.json'

id: 'playground'
language: typescript
layer: application
stack: backend

dependsOn:
  - 'inertia-hono'

tasks:
  build:
    command: 'bun run build'
    deps:
      - 'inertia-hono:build'
    inputs:
      - 'src/**/*'
      - 'vite.config.ts'
      - 'package.json'
      - 'project://inertia-hono'
    outputs:
      - 'dist'

  dev:
    options:
      envFile: '/.env'
    command: 'bun run dev'
    preset: server      
    deps:
      - 'inertia-hono:build'
    inputs:
      - 'src/**/*'
      - 'vite.config.ts'
      - 'package.json'

  test:
    command: 'bun run test'
    inputs:
      - 'src/**/*'
      - 'vitest.config.ts'
      - 'vite.config.ts'
      - 'package.json'

  lint:
    command: 'bun run lint:playground'
    options:
      runFromWorkspaceRoot: true
    inputs:
      - 'src/**/*'
      - 'src/**/*.vue'
      - 'vite.config.ts'
      - '/eslint.config.js'
      - 'package.json'
